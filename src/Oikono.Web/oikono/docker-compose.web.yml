version: "3.8"

networks:
  oikono-backend:
    name: oikono-backend

services:
  oikono.web:
    container_name: oikono.web
    build:
      context: .
      dockerfile: /Dockerfile
    image: oikono.web
    restart: always
    ports:
      - "3000:3000"
    networks:
      - oikono-backend
